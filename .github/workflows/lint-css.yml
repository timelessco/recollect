name: Stylelint

on:
  pull_request: ~

  push:
    branches:
      - main

jobs:
  stylelint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/prepare
      - run: pnpm lint:css
