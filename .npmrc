## PNPM related ###############
## https://pnpm.io/npmrc      #
###############################

# Pinlock to exact version (default is '^')
# https://pnpm.io/npmrc#save-prefix
# see also how save-workspace-protocol affect this https://pnpm.io/npmrc#save-workspace-protocol
save-prefix=''

## Security Configuration #####
## https://pnpm.io/npmrc#ignore-scripts
## https://pnpm.io/npmrc#package-import-method

# Disable lifecycle scripts globally (preinstall, postinstall, etc.)
# Only packages listed in package.json's pnpm.onlyBuiltDependencies can run scripts
# This prevents malicious packages from executing arbitrary code during installation
ignore-scripts=true

# Wait 3 days before using newly published packages
# This gives the security community time to detect and remove malicious releases
# https://pnpm.io/npmrc#package-import-method
minimumReleaseAge=3d
