# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Once you create a project you need to add the following keys that is given by supabase to the project
# When in production the app will point out to these keys

# PROD (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

SUPABASE_SERVICE_KEY=your-supabase-service-key

# OR

# STAGING (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

SUPABASE_SERVICE_KEY=your-supabase-service-key

# DEV (OPTIONAL - Required for Local/Dev branch Development)
# If you want the app to point to different Supabase projects based on dev and prod
# These env variables will only be used in development mode (pnpm run dev)
# For local Supabase setup, use: http://127.0.0.1:54323
# Get keys from: npx supabase status
NEXT_PUBLIC_DEV_SUPABASE_URL=your-dev-supabase-url
NEXT_PUBLIC_DEV_SUPABASE_ANON_KEY=your-dev-supabase-anon-key

DEV_SUPABASE_SERVICE_KEY=your-dev-supabase-service-key

# Required for Local/Dev branch Development to perform Google Auth
DEV_SUPABASE_AUTH_GOOGLE_CLIENT_ID=your-dev-supabase-auth-google-client-id
DEV_SUPABASE_AUTH_GOOGLE_SECRET=your-dev-supabase-auth-google-secret

# =============================================================================
# APPLICATION URLS
# =============================================================================
# The url after app is deployed on vercel, used for sitemap (OPTIONAL, if not given then the site map will not be generated)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# AI SERVICES
# =============================================================================
# OPTIONAL: Used to generate the embeddings for AI related tasks
GOOGLE_GEMINI_TOKEN=your-google-gemini-api-key

# =============================================================================
# CLOUDFLARE
# =============================================================================
# Cloudflare bucket keys (REQUIRED)
NEXT_PUBLIC_CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
NEXT_PUBLIC_CLOUDFLARE_ACCESS_KEY_ID=your-cloudflare-access-key-id
NEXT_PUBLIC_CLOUDFLARE_SECRET_ACCESS_KEY=your-cloudflare-secret-access-key
NEXT_PUBLIC_CLOUDFLARE_PUBLIC_BUCKET_URL=your-cloudflare-bucket-url

# =============================================================================
# EMAIL SERVICES
# =============================================================================
# OPTIONAL: Used for sending transactional emails via Resend
RESEND_KEY=your-resend-api-key

# =============================================================================
# MONITORING & ANALYTICS
# =============================================================================
# OPTIONAL: If not provided, sentry will not work
SENTRY_DSN=your-sentry-dsn
UMAMI_ID=your-umami-website-id
UMAMI_SRC=your-umami-script-url

# =============================================================================
# SECURITY
# =============================================================================
API_KEY_ENCRYPTION_KEY=your-secret-key
PDF_SECRET_KET=PDF-secret-key